// Rotas do grupo principal (main) — responsivo: tabs em telas compactas,
// drawer no desktop web. Nunca hardcodar "/(auth)/(main)/..." fora daqui.
export const LAUNCHES_PATH = "/(auth)/(main)/launches";
export const FINANCE_PATH = "/(auth)/(main)/finance";
